import type { Metadata } from "next";
import SiteHeader from "@/components/SiteHeader";
import SiteFooter from "@/components/SiteFooter";
import HtmlPage from "@/components/HtmlPage";
import content from "./content.json";

export const metadata: Metadata = {
  title: "Scent Boosted. Planet Protected. — Me Mother Earth",
  description: "Infuse your laundry with a clean, mineral-based fragrance that lasts for weeks. Our microplastic-free beads dissolve completely, leaving only a fresh scent behind.",
};

export default function Page() {
  return (
    <>
      <SiteHeader sections={content.sections} />
      <main>
        <HtmlPage html={content.html} scripts={content.scripts} />
      </main>
      <SiteFooter sections={content.sections} />
    </>
  );
}
